services:
  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - backendnew

  backendnew:
    build: ./backendnew
    ports:
      - "5000:5000"
    env_file:
      - ./backendnew/.env